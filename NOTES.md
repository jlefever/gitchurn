# Notes

## Potential Options

```
--git-bin
--ctags-bin
--git-repo or -C
--git-log-args
--exclude-paths
--max-changes
--langs
--only-files-from
```

## Distribute

```
python3 -m build
python3 -m twine upload --repository testpypi dist/*
```
